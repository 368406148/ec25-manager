{
  "name": "ec25-manager",
  "version": "0.3.0",
  "description": "EC25 / Baiwang USB modem manager for the macOS menu bar",
  "author": "nickspace",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"EC25 Manager\" --platform=darwin --arch=arm64 --out=../dist --overwrite --app-bundle-id=one.nickspace.ec25-manager --app-version=0.3.0 --icon=assets/icon.icns --prune=true"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "@electron/packager": "^18.3.6"
  }
}
